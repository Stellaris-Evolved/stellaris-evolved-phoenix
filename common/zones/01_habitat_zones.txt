@base_buildtime = 10
@base_cost = 300
@base_upkeep = 2

zone_habitat_knights = {
	icon = GFX_job_knight_commander
	base_buildtime = @base_buildtime
	potential = { # planet scope
		exists = owner
		owner = { has_origin = origin_toxic_knights }
		OR = {
			has_planet_flag = toxic_knights_habitat
			has_building = building_order_castle
		}
	}

	# TODO AUG-52066 Swap to fortress zone if no longer valid

	unlock = { # planet scope
		exists = owner
		owner = { has_origin = origin_toxic_knights }
		OR = {
			has_planet_flag = toxic_knights_habitat
			has_building = building_order_castle
		}
	}

	resources = {
		category = planet_zones
		cost = {
			minerals = @base_cost
		}
		upkeep = {
			energy = @base_upkeep
		}
	}

	max_buildings = 3

	include = {
		all
	}
	# TODO Knight Zone building restrictions
	# inline_script = "zones/zone_urban_include"

	# inline_script = "zones/zone_urban_exclude"

    triggered_district_planet_modifier = {
		potential = {
			always = yes
		}
        job_squire_add = @scaling_district_1_job
		mult = owner.value:hab_void_dweller_jobs
    }
    triggered_desc = {
        text = job_squire_effect_desc
    }

	planet_modifier = {
		zone_building_slots_add = 3
	}
}

zone_habitat_food = {
	icon = GFX_resource_food_large
	base_buildtime = @base_buildtime
	potential = { # planet scope
		always = yes
	}

	unlock = { # planet scope
		always = yes
	}
	resources = {
		category = planet_zones
		cost = {
			minerals = @base_cost
		}
		upkeep = {
			energy = @base_upkeep
		}
	}

	inline_script = "zones/zone_food_include"

	inline_script = {
		script = jobs/zone_farmers_add
		AMOUNT = @scaling_district_1_job
	}

	planet_modifier = {
		zone_building_slots_add = 3
	}
}

zone_habitat_minerals = {
	icon = GFX_resource_minerals_large
	base_buildtime = @base_buildtime

	potential = { # planet scope
		always = yes
	}

	unlock = { # planet scope
		always = yes
	}

	resources = {
		category = planet_zones
		cost = {
			minerals = @base_cost
		}
		upkeep = {
			energy = @base_upkeep
		}
	}

	inline_script = "zones/zone_minerals_include"

	planet_modifier = {
		zone_building_slots_add = 3
	}
}

zone_habitat_energy = {
	icon = GFX_resource_energy_large
	base_buildtime = @base_buildtime
	resources = {
		category = planet_zones
		cost = {
			minerals = @base_cost
		}
		upkeep = {
			energy = @base_upkeep
		}
	}

	inline_script = "zones/zone_energy_include"

	planet_modifier = {
		zone_building_slots_add = 3
	}
}

zone_habitat_research = {
	icon = GFX_colony_type_research
	base_buildtime = @base_buildtime
	potential = { # planet scope
		always = yes
	}

    unlock = { # planet scope
		always = yes
	}

	resources = {
		category = planet_zones
		cost = {
			minerals = @base_cost
		}
		upkeep = {
			energy = @base_upkeep
		}
	}

	inline_script = "zones/zone_research_include"

	planet_modifier = {
		zone_building_slots_add = 3
	}
}
